language: python
python:
      - "3.7"
addons:
      apt_packages:
              - swig
              - python-dev
cache: pip

install:
      - pip install pipenv
      - pipenv install
script: 
      - pipenv shell
      - pytest -v --pep8
      - PYTHONPATH=.. sphinx-build -nT -b dummy docs/ docs/_build/html
      - cd post
      - make 
      - cd create_disp_dat_c
      - make create_disp_dat
      - make test
