language: python
python:
      - "3.6"
addons:
      apt_packages:
              - swig
              - python-dev
              - texlive
install:
      - pip install -r requirements.txt
script: 
      - pytest -v --pep8
      - PYTHONPATH=$HOME sphinx-build -b dummy docs/ docs/_build/latex
      - cd post/create_disp_dat_c
      - make
      - make test
