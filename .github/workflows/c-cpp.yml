name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: cd fem/post/create_disp_dat_c
      run: cd fem/post/create_disp_dat_c
    - name: make
      run: make
    - name: run test
      run: make test
